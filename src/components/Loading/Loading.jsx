import styles from "./loadingStyles.module.css";
// load prop is a boolean, generate an animation while load is false

function Loading() {
  return (
    
    <div>
      Loading...
    </div>
  )
}

export default Loading